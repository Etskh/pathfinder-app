
import { CreateActions } from '../lib/Actions';

export const SpellActions = CreateActions({
  startSpellDrag: (payload) => {
    // empty
  },
  selectSpellSlot: (payload) => {
    // empty
  },
  cancelSpellDrag: (payload) => {
    // empty
  },
});
